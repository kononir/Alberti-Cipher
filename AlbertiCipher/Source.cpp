// AlbertiCipher.cpp : This file contains the 'main' function. Program execution begins and ends there.?//??#include "pch.h"?#include <iostream>?#include <string>?#include "AlbertiDisk.h"??using namespace std;??string alphabet = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890";??int main()?{?	AlbertiDisk disk = AlbertiDisk(alphabet, alphabet);??	int choice;?	string message, ciphertext, decryptedMessage;?	while (true)?	{?		cout << "1 - Show alberti disk, 2 - Encrypt message, 3 - Turn disk right, 4 - Turn disk left, 0 - Exit";?		cin >> choice;??		switch (choice)?		{?		case 1:?			cout << "Disk:" << endl;?			cout << disk.printDisk() << endl;?			break;?		case 2:?			cout << "Enter message:" << endl;?			cin >> message;??			ciphertext = disk.encryptText(message);?			cout << "Ciphertext:" << endl;?			cout << ciphertext << endl;??			decryptedMessage = disk.encryptText(message);?			cout << "Decrypted message:" << endl;?			cout << decryptedMessage << endl;?			break;?		case 3:?			disk.turnMovedDiskRight();?			break;?		case 4:?			disk.turnMovedDiskLeft();?			break;?		case 0:?			system("exit");?		default:?			break;?		}?	}?}